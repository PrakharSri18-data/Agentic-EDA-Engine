[tool.ruff]
line-length = 120
target-version = "py311"
# Demo/sample notebooks are artifacts, not source we want auto-formatted.
extend-exclude = ["*.ipynb"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
ignore = ["E501"]

[tool.pytest.ini_options]
testpaths = ["tests"]
