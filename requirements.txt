# Direct dependencies only (previously this was a 72-line pip freeze full of
# Streamlit's transitive deps). Versions match the original working freeze;
# pip resolves the transitive tree.
langgraph==1.1.6
langchain-ollama==1.1.0
streamlit==1.56.0
pandas==3.0.2
matplotlib==3.10.8
seaborn==0.13.2
openpyxl==3.1.5
tabulate==0.10.0
